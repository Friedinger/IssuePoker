id: add roles
author: Default
realm: local_realm
changes:
  - addRole:
      name: user
      description: Can see issues and vote for them.
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: admin
      description: Can create issues, reset votes and reveal them.
      clientRole: true
      clientId: local
      composite: false